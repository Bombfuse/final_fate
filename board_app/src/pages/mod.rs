//! UI pages (screens) for the app.
//!
//! Each page exposes a `render(...)` function that draws its UI into an egui `Ui`.

pub mod empty;
pub mod unit_edit;

// Future pages:
// pub mod main_menu;
// pub mod scenario_edit;
// pub mod item_edit;
// pub mod simulation;
