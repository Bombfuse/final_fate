{
  "name": "simulator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Hex-grid + card-deck simulator (Vite + Vitest).",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "@vitest/ui": "^2.1.8",
    "jsdom": "^25.0.1",
    "vite": "^6.1.0",
    "vitest": "^2.1.8"
  }
}
